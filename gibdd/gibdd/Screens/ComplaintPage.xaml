<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="gibdd.ComplaintPage">

        <StackLayout>
            <Label Text="Текст обращения:"/>
            <Editor x:Name="editor" HeightRequest="150" AutoSize="TextChanges"/>
            <StackLayout Orientation="Horizontal">
                <Button Text="Сфотать" Clicked="takePhoto"/>
                <Button Text="Из галлереи" Clicked="pickPhoto"/>
            </StackLayout>
            
            <ScrollView>
                <StackLayout>
                    <ListView x:Name="imgList" 
                              ItemsSource="{Binding}"
                              ItemTapped="deleteImage">
                        <ListView.ItemTemplate> 
                            <DataTemplate>
                                <ViewCell> 
                                    <StackLayout>  
                                        <Image Source="{Binding imageSource}" HeightRequest="200" WidthRequest="200"></Image>  
                                    </StackLayout>  
                                </ViewCell>  
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <Label Text="testLabel" />
                </StackLayout>
            </ScrollView>
                
        </StackLayout>
    
</ContentPage>